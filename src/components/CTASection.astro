---
interface Props {
  title?: string;
  subtitle?: string;
  buttonText?: string;
  type?: 'primary' | 'success' | 'dark';
}

const { 
  title = "THE SWAMP IS GROWING FAST", 
  subtitle = "Don't miss the chance to be part of the most powerful meme token on Solana. SECURE YOUR BAG NOW.", 
  buttonText = "BUY $SHREK BEFORE IT'S GONE",
  type = "success"
} = Astro.props;

const btnClass = type === 'success' ? 'btn-success' : type === 'primary' ? 'btn-primary' : 'btn-outline';
---

<section class={`cta-block ${type}`}>
  <div class="container cta-container">
    <div class="cta-content">
      <h2>{title}</h2>
      <p>{subtitle}</p>
    </div>
    <div class="cta-action">
      <a href="#buy" class={`btn ${btnClass} cta-btn`}>{buttonText}</a>
    </div>
  </div>
</section>

<style>
  .cta-block {
    padding: 3rem 0;
    margin: 1rem 0;
    position: relative;
    overflow: hidden;
  }

  .cta-block::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at center, rgba(164, 198, 57, 0.1) 0%, transparent 70%);
    z-index: -1;
  }

  .primary {
    background: linear-gradient(90deg, rgba(164, 198, 57, 0.05), rgba(122, 193, 67, 0.05));
    border-top: 1px solid rgba(164, 198, 57, 0.1);
    border-bottom: 1px solid rgba(164, 198, 57, 0.1);
  }

  .success {
    background: linear-gradient(90deg, rgba(164, 198, 57, 0.1), transparent);
    border-left: 8px solid #A4C639;
    box-shadow: inset 20px 0 30px -10px rgba(164, 198, 57, 0.1);
  }

  .dark {
    background: #0D0D0D;
    border-top: 1px solid var(--glass-border);
    border-bottom: 1px solid var(--glass-border);
  }

  .cta-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
  }

  .cta-content h2 {
    font-size: 1.8rem;
    margin-bottom: 0.5rem;
    letter-spacing: 0.05em;
  }

  .cta-content p {
    color: var(--text-muted);
    font-size: 1rem;
    max-width: 600px;
  }

  .cta-btn {
    padding: 1.5rem 4rem;
    font-size: 1.25rem;
    white-space: nowrap;
    box-shadow: 0 10px 40px rgba(164, 198, 57, 0.3);
    text-shadow: 0 2px 4px rgba(0,0,0,0.3);
  }

  .cta-btn:hover {
    transform: scale(1.05) rotate(-1deg);
    box-shadow: 0 20px 60px rgba(164, 198, 57, 0.5);
  }

  @media (max-width: 900px) {
    .cta-container {
      flex-direction: column;
      text-align: center;
    }
    
    .cta-content h2 {
      font-size: 1.5rem;
    }
    
    .cta-btn {
      width: 100%;
    }
  }
</style>
